grep -o -b "the" file1.txt # Displays the matching position and the given pattern
# Display format is:
# starting index of the match : pattern
# index starts from 0 and continues after the line break as well. (second line index won't start with 0)
