sort cities.txt > cities_sorted.txt # Creates a file named cities_sorted.txt and saves the result
# sort -o cities.txt cities_sorted.txt # This one is not that smart, we need the file in advance, otherwise it will fail
